apiVersion: v1
kind: Service
metadata:
  name: aceest-blue-green-service
  namespace: aceest-devops
spec:
  type: NodePort
  selector:
    app: aceest-fitness-app
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30081