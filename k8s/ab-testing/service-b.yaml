apiVersion: v1
kind: Service
metadata:
  name: aceest-ab-b-service
  namespace: aceest-devops
spec:
  type: NodePort
  selector:
    app: aceest-ab-app
    version: b
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30086