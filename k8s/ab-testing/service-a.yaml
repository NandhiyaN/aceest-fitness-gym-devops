apiVersion: v1
kind: Service
metadata:
  name: aceest-ab-a-service
  namespace: aceest-devops
spec:
  type: NodePort
  selector:
    app: aceest-ab-app
    version: a
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30085